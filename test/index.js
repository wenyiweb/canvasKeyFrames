'use strict';
require('should');
const CanvasKeyframes = require('../src/canvas-keyframes');

describe('CanvasKeyframes Test', () => {
  it('should get "CanvasKeyframes"', () => {
    console.log(CanvasKeyframes)
  });
});